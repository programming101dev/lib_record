cmake_minimum_required(VERSION 3.14)
project(p101_record_tests C)
enable_testing()
set(CMAKE_C_STANDARD 17)
add_library(record_under_test STATIC ../src/record.c ../src/event_write.c)
target_compile_definitions(record_under_test PRIVATE P101_RECORD_TESTING=1)
target_include_directories(record_under_test PUBLIC ../include)
add_executable(test_record test_record.c)
target_link_libraries(test_record PRIVATE record_under_test)
add_test(NAME test_record COMMAND test_record)
